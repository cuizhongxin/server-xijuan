<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.ProductionMapper">

    <select id="findByUserId" resultType="java.lang.String">
        SELECT data FROM production WHERE od_user_id = #{odUserId}
    </select>

    <insert id="upsert">
        INSERT INTO production (od_user_id, data)
        VALUES (#{odUserId}, #{data})
        ON DUPLICATE KEY UPDATE data = #{data}
    </insert>

</mapper>
