<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.SecretRealmRewardMapper">

    <select id="findByRealmId" resultType="java.util.HashMap">
        SELECT id, realm_id, reward_type, item_id, name, icon, item_sub_type,
               quality, drop_weight, equip_pre_id, item_pre_id,
               position, set_name, set_effect_3, set_effect_6,
               attack, defense, soldier_hp, mobility, description, sort_order
        FROM secret_realm_reward
        WHERE realm_id = #{realmId} AND status = 1
        ORDER BY sort_order ASC
    </select>

    <select id="findByRealmIdAndType" resultType="java.util.HashMap">
        SELECT id, realm_id, reward_type, item_id, name, icon, item_sub_type,
               quality, drop_weight, equip_pre_id, item_pre_id,
               position, set_name, set_effect_3, set_effect_6,
               attack, defense, soldier_hp, mobility, description, sort_order
        FROM secret_realm_reward
        WHERE realm_id = #{realmId} AND reward_type = #{rewardType} AND status = 1
        ORDER BY sort_order ASC
    </select>

    <select id="findAllEnabled" resultType="java.util.HashMap">
        SELECT id, realm_id, reward_type, item_id, name, icon, item_sub_type,
               quality, drop_weight, equip_pre_id, item_pre_id,
               position, set_name, set_effect_3, set_effect_6,
               attack, defense, soldier_hp, mobility, description, sort_order
        FROM secret_realm_reward
        WHERE status = 1
        ORDER BY realm_id, sort_order ASC
    </select>

</mapper>
