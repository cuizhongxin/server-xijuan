<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.RewardIssueLogMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO reward_issue_log (
            biz_type, biz_id, target_id, server_id, extra, create_time
        )
        VALUES (
            #{bizType}, #{bizId}, #{targetId}, #{serverId}, #{extra}, #{createTime}
        )
    </insert>

</mapper>
