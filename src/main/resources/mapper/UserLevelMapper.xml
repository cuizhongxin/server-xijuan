<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.UserLevelMapper">

    <select id="findByUserId" resultType="java.lang.String">
        SELECT data FROM user_level WHERE user_id = #{userId}
    </select>

    <insert id="upsert">
        INSERT INTO user_level (user_id, data, create_time, update_time)
        VALUES (#{userId}, #{data}, #{createTime}, #{updateTime})
        ON DUPLICATE KEY UPDATE data = #{data}, update_time = #{updateTime}
    </insert>

    <delete id="deleteByUserId">
        DELETE FROM user_level WHERE user_id = #{userId}
    </delete>

</mapper>
