<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.SecretRealmConfigMapper">

    <select id="findAll" resultType="java.util.HashMap">
        SELECT id, name, description, min_level, cost_gold, equip_set_name,
               equip_base_rate, pity_count, daily_limit, sort_order, status
        FROM secret_realm_config
        WHERE status = 1
        ORDER BY sort_order ASC
    </select>

    <select id="findById" resultType="java.util.HashMap">
        SELECT id, name, description, min_level, cost_gold, equip_set_name,
               equip_base_rate, pity_count, daily_limit, sort_order, status
        FROM secret_realm_config
        WHERE id = #{id}
    </select>

</mapper>
