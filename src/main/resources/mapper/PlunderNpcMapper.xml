<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.PlunderNpcMapper">

    <select id="findAll" resultType="java.util.HashMap">
        SELECT id, name, faction, bonus_resource AS bonusResource,
               sort_order AS sortOrder, power_base AS powerBase, power_extra AS powerExtra
        FROM plunder_npc
        ORDER BY faction, sort_order
    </select>

    <select id="findByFaction" resultType="java.util.HashMap">
        SELECT id, name, faction, bonus_resource AS bonusResource,
               sort_order AS sortOrder, power_base AS powerBase, power_extra AS powerExtra
        FROM plunder_npc
        WHERE faction = #{faction}
        ORDER BY sort_order
    </select>

</mapper>
