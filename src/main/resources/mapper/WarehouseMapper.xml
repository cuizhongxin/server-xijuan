<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.WarehouseMapper">

    <select id="findByUserId" resultType="java.lang.String">
        SELECT data FROM warehouse WHERE user_id = #{userId} LIMIT 1
    </select>

    <insert id="upsert">
        INSERT INTO warehouse (id, user_id, data, create_time, update_time)
        VALUES (#{id}, #{userId}, #{data}, #{createTime}, #{updateTime})
        ON DUPLICATE KEY UPDATE data = #{data}, update_time = #{updateTime}
    </insert>

</mapper>
