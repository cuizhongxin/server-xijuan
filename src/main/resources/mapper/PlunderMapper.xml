<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.PlunderMapper">

    <resultMap id="plunderDataMap" type="com.tencent.wxcloudrun.model.PlunderData">
        <id property="userId" column="user_id"/>
        <result property="todayCount" column="today_count"/>
        <result property="todayPurchased" column="today_purchased"/>
        <result property="lastResetDate" column="last_reset_date"/>
    </resultMap>

    <select id="findByUserId" resultMap="plunderDataMap">
        SELECT user_id, today_count, today_purchased, last_reset_date
        FROM plunder_data WHERE user_id = #{userId}
    </select>

    <insert id="upsert">
        INSERT INTO plunder_data (user_id, today_count, today_purchased, last_reset_date, create_time, update_time)
        VALUES (#{userId}, #{todayCount}, #{todayPurchased}, #{lastResetDate}, #{createTime}, #{updateTime})
        ON DUPLICATE KEY UPDATE
            today_count = #{todayCount},
            today_purchased = #{todayPurchased},
            last_reset_date = #{lastResetDate},
            update_time = #{updateTime}
    </insert>

    <select id="findAllUserLevels" resultType="java.util.HashMap">
        SELECT od_user_id AS userId, level, silver, wood, paper, food, `rank`
        FROM user_resource
    </select>

</mapper>
