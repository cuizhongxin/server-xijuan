<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.RechargeOrderMapper">

    <select id="findById" resultType="java.lang.String">
        SELECT data FROM recharge_order WHERE id = #{id}
    </select>

    <select id="findByUserId" resultType="java.util.HashMap">
        SELECT id, data FROM recharge_order WHERE od_user_id = #{odUserId} ORDER BY create_time DESC
    </select>

    <insert id="upsert">
        INSERT INTO recharge_order (id, od_user_id, status, data, create_time)
        VALUES (#{id}, #{odUserId}, #{status}, #{data}, #{createTime})
        ON DUPLICATE KEY UPDATE status = #{status}, data = #{data}
    </insert>

</mapper>
