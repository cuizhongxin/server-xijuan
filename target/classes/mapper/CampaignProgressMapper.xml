<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.CampaignProgressMapper">

    <resultMap id="campaignProgressMap" type="com.tencent.wxcloudrun.model.CampaignProgress">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="campaignId" column="campaign_id"/>
        <result property="currentStage" column="current_stage"/>
        <result property="maxClearedStage" column="max_cleared_stage"/>
        <result property="todayChallengeCount" column="today_challenge_count"/>
        <result property="todayDate" column="today_date"/>
        <result property="status" column="status"/>
        <result property="currentTroops" column="current_troops"/>
        <result property="maxTroops" column="max_troops"/>
        <result property="reviveCount" column="revive_count"/>
        <result property="generalId" column="general_id"/>
        <result property="fullCleared" column="full_cleared"/>
        <result property="totalExpGained" column="total_exp_gained"/>
        <result property="totalSilverGained" column="total_silver_gained"/>
        <result property="startTime" column="start_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findByUserIdAndCampaignId" resultMap="campaignProgressMap">
        SELECT * FROM campaign_progress WHERE user_id = #{userId} AND campaign_id = #{campaignId}
    </select>

    <select id="findAllByUserId" resultMap="campaignProgressMap">
        SELECT * FROM campaign_progress WHERE user_id = #{userId}
    </select>

    <insert id="upsert">
        INSERT INTO campaign_progress (user_id, campaign_id, current_stage, max_cleared_stage,
            today_challenge_count, today_date, status, current_troops, max_troops, revive_count,
            general_id, full_cleared, total_exp_gained, total_silver_gained, start_time, update_time)
        VALUES (#{userId}, #{campaignId}, #{currentStage}, #{maxClearedStage},
            #{todayChallengeCount}, #{todayDate}, #{status}, #{currentTroops}, #{maxTroops}, #{reviveCount},
            #{generalId}, #{fullCleared}, #{totalExpGained}, #{totalSilverGained}, #{startTime}, #{updateTime})
        ON DUPLICATE KEY UPDATE
            current_stage = #{currentStage}, max_cleared_stage = #{maxClearedStage},
            today_challenge_count = #{todayChallengeCount}, today_date = #{todayDate}, status = #{status},
            current_troops = #{currentTroops}, max_troops = #{maxTroops}, revive_count = #{reviveCount},
            general_id = #{generalId}, full_cleared = #{fullCleared},
            total_exp_gained = #{totalExpGained}, total_silver_gained = #{totalSilverGained},
            update_time = #{updateTime}
    </insert>

    <delete id="deleteByUserIdAndCampaignId">
        DELETE FROM campaign_progress WHERE user_id = #{userId} AND campaign_id = #{campaignId}
    </delete>

</mapper>
